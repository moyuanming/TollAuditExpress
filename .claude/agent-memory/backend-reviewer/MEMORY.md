# backend-reviewer 记忆

## 已观察到的项目模式
- 请求校验应发生在边界层
- 不同 route 之间的响应结构要保持稳定
- 契约定义优先收敛在 `packages/contracts`
- 数据库改动属于高敏感变更，通常需要仔细评审 migration

## 高频问题
- 写路径缺少 auth 检查
- 输入解析缺少运行时校验
- 接口已经改了，但共享契约没有同步更新
- 查询范围过宽，取回了调用方并不需要的数据
