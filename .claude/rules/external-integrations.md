---
paths:
  - "apps/api/src/**/*.ts"
  - "apps/api/src/**/*.tsx"
---

# 外部集成规则

- 第三方 API 要包在职责清晰、类型明确的 client 或 adapter 后面。
- 厂商特定的响应结构进入共享领域逻辑或契约层前，先做统一整理。
- 重试、超时、auth header 和退避策略尽量收在集成边界附近。
- 非 2xx 响应、部分失败和限流场景都要显式处理。
- 不要把同一个外部服务的原始 `fetch` 调用分散到多个模块里。
