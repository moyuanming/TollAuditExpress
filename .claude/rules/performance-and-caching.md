---
paths:
  - "apps/web/src/**/*.{ts,tsx}"
  - "apps/api/src/**/*.{ts,tsx}"
---

# 性能与缓存规则

- 当前端服务端渲染或后端聚合已经足够时，不要引入不必要的客户端复杂度。
- 数据获取和高成本计算的缓存边界要写清楚。
- 注意重复 fetch、bundle 体积激增和可避免的 rerender。
- 不影响首屏时，把非关键工作延后处理。
- 在为了微优化增加复杂度前，先测量真实收益。
