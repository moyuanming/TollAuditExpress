---
paths:
  - "packages/contracts/**/*.ts"
  - "packages/contracts/**/*.tsx"
---

# 共享契约规则

- `packages/contracts` 是前后端共享契约的单一事实来源。
- 请求、响应和关键 DTO 只要可行，都优先放到这里统一定义。
- 契约改动时，要同时检查前端调用方、后端实现和测试。
- 不要在前端和后端各自维护一份语义相同但逐渐漂移的接口类型。
- 破坏性契约变更要在总结中明确标出影响范围和迁移建议。
