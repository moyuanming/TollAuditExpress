# 共享契约规则

- `packages/contracts` 是前后端共享结构定义、DTO 和类型的单一事实来源。
- 只要可行，客户端和服务端都依赖同一套契约定义或等价结构校验机制。
- 契约一旦变更，要同步检查前端调用方、后端实现和测试。
- 不要在 `apps/web` 或 `apps/api` 中各自维护一份语义相同但实现漂移的接口类型。
- 向后兼容要求不明确时，先在总结里说清楚破坏范围和迁移建议。
