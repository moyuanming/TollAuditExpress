fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
pymysql>=1.1.0
requests>=2.31.0
python-multipart>=0.0.6
numpy>=1.24.0
python-dotenv>=1.0.0
starlette>=0.27.0

# Vehicle AI capability is now provided by the standalone vehicle-ai-service
# (deployed on the same target machine, port 8081). The main API only needs
# the HTTP client below. Torch / torchvision / ultralytics / opencv / zhipuai /
# openai have been moved to the public service and are no longer required here.
httpx>=0.25.0
tenacity>=8.2.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Auth
PyJWT[crypto]>=2.8.0
