# API 后端规则

- `apps/api` 负责请求边界、auth、业务逻辑、数据库访问和第三方集成。
- 输入校验、认证、授权和限流优先放在请求边界附近完成。
- 共享契约优先放在 `packages/contracts`，不要在请求入口里重复定义漂移的请求或响应结构。
- 业务逻辑优先下沉到可复用模块，不要把关键逻辑堆在入口文件里。
- 外部服务集成、超时、重试、退避和错误整理都尽量收在后端边界附近。
- 高风险写路径要优先补测试和回归验证建议。
