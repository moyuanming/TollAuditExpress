# Web 前端规则

- `apps/web` 只负责页面、组件、浏览器交互和前端状态编排。
- 前端不要直接访问数据库，也不要绕过共享契约层拼接后端请求。
- 页面和组件优先复用现有 UI 基础组件、共享 token 和设计模式。
- 只要可行，数据请求都通过共享 contract 和项目内 API client 对接后端。
- loading、empty、error 和权限受限状态都要显式处理。
- 如果组件状态越来越多、逻辑越来越复杂，就把视图和状态编排拆开。
